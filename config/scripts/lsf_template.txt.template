#!/bin/bash
#BSUB -P emwrap-tomo
{gpu_line}
#BSUB -R "rusage[mem=5000]"
#BSUB -q cryo_core
#BSUB -n {cpus}
#BSUB -R "span[ptile={cpus}]"
#BSUB -e {working_dir}/{jobId}/run.err -o {working_dir}/{jobId}/run.out

cd {working_dir}

hostname -f

# For debugging, avoid cleanning temporary batch folders
export EMWRAP_CLEAN=0
source /etc/bashrc.modules

{command}

