#!/bin/bash


# Get the directory of the current script
DIR="$(dirname "$(readlink -f "$0")")"

# Source the other script using the determined directory
. "${DIR}/../emwrap.bashrc"

python -m emwrap.tests.mock "$@"
exit_code=$?

echo "--------------------------------"
echo "Mock WarpApoF exit code: $exit_code"
echo "--------------------------------"

exit "$exit_code"
